(()=>{const session=()=>{try{return JSON.parse(localStorage.getItem('sentinelAdminSession')||localStorage.getItem('sentinelBuyerSession')||'null')}catch{return null}},esc=v=>String(v??'').replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));async function render(){if(location.pathname!=='/deals')return;const root=document.querySelector('#app');if(!root||root.dataset.deals)return;root.dataset.deals='loading';try{const s=session();if(!s)throw Error('Enter your name or sign in to view recovery requests.');const response=await fetch('/api/deals',{headers:{Authorization:`Bearer ${s.token}`}}),items=await response.json();if(!response.ok)throw Error(items.error||'Unable to load refund requests.');root.innerHTML=`<div class="deals-page"><header><a href="/shop">NEXORA.</a><a href="/shop">Back to shop</a></header><main><p>PAYMENT RECOVERY</p><h1>Refund requests</h1><section>${items.length?items.map(x=>`<article><span>${esc(x.status)}</span><h2>${esc(x.productName)}</h2><p>Customer: ${esc(x.customerName)} · Amount: $${Number(x.amount||0).toFixed(2)}</p><p>Original order: ${esc(x.originalOrderId)}</p><p>Settlement mismatch: customer debited, organization not credited.</p><small>Provider verification: ${esc(x.verificationStatus)}</small></article>`).join(''):'<div class="empty"><h2>No refund requests</h2><p>Customer refund selections will appear here.</p></div>'}</section></main></div>`}catch(e){root.innerHTML=`<div class="deals-page"><main><h1>Refund requests unavailable</h1><p>${esc(e.message)}</p></main></div>`}finally{root.dataset.deals='done'}}new MutationObserver(render).observe(document.querySelector('#app'),{childList:true,subtree:true});render()})();
