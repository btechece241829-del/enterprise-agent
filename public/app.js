const app=document.querySelector('#app'),q=s=>document.querySelector(s),savedSession=JSON.parse(localStorage.getItem('sentinelSession')||'null'),state={session:savedSession,buyerSession:JSON.parse(localStorage.getItem('sentinelBuyerSession')||'null')||(savedSession?.role==='BUYER'?savedSession:null),adminSession:JSON.parse(localStorage.getItem('sentinelAdminSession')||'null')||(savedSession?.role!=='BUYER'?savedSession:null),products:[]};
const api=async(p,o={})=>{let r=await fetch(p,{...o,headers:{'Content-Type':'application/json',...(state.session?{Authorization:'Bearer '+state.session.token}:{})}}),x=await r.json().catch(()=>({}));if(!r.ok)throw Error(x.error||'Request failed');return x},money=n=>'$'+(+n||0).toFixed(2),esc=x=>String(x||'').replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));
const go=p=>{history.pushState({},'',p);render()},toast=t=>{let x=document.createElement('div');x.className='toast';x.textContent=t;document.body.append(x);setTimeout(()=>x.remove(),2400)};
function header(){return `<div class="utility"><div class="page-width"><span>Free shipping on orders over $75</span><span>Support · Easy returns · <a href="/login">Operations sign in</a></span></div></div><header class="site-header"><div class="page-width header-main"><a class="wordmark" href="/shop">NEXORA<span>.</span></a><label class="search">⌕<input id="site-search" placeholder="Search headphones, phones, cameras…"></label><nav class="header-actions"><a href="/profile">◉<small>Profile</small></a><button>♡<small>Saved</small></button><a href="/cart">🛒<small>Cart</small></a></nav></div><nav class="category-nav"><div class="page-width"><a href="/products">All products</a><a href="/products?category=Audio">Audio</a><a href="/products?category=Wearables">Wearables</a><a href="/products?category=Accessories">Computers</a><a href="/products?category=Cameras">Cameras</a><a href="/products?category=Lifestyle">Lifestyle</a><a class="deal-link" href="/products?sort=price">Deals</a></div></nav></header>`}
function footer(){return `<footer><div class="page-width footer-grid"><div><a class="wordmark" href="/shop">NEXORA<span>.</span></a><p>Technology selected for the way you live, work and create.</p></div><div><b>Shop</b><a href="/products">All products</a><a href="/products?category=Audio">Audio</a><a href="/products?category=Cameras">Cameras</a></div><div><b>Customer care</b><a href="/profile">My account</a><a href="/orders">Track orders</a><a href="/cart">Shipping & returns</a></div><div><b>Get updates</b><p>New releases and considered deals.</p><div class="subscribe"><input placeholder="Email address"><button>→</button></div></div></div><div class="page-width footer-bottom">© 2026 Nexora Electronics <span>Secure checkout · Demo simulation</span></div></footer>`}const page=x=>header()+`<main>${x}</main>`+footer();
async function products(){if(!state.products.length)state.products=await api('/api/products');return state.products}
function card(p){let price=p.price*(1-p.discount/100);return `<article class="product-card"><div class="product-photo"><span class="product-badge">${p.discount?p.discount+'% off':'New'}</span><button class="save-product">♡</button><img src="${p.image}" alt="${esc(p.name)}" loading="lazy"><button class="quick-add" onclick="add('${p.id}')">Quick add</button></div><div class="product-copy"><small>${esc(p.category)} · NEXORA SELECT</small><h3><a href="/product/${p.id}">${esc(p.name)}</a></h3><div class="rating">★ ${p.rating} <em>· verified reviews</em></div><div class="price-row"><strong>${money(price)}</strong>${p.discount?`<del>${money(p.price)}</del>`:''}<button onclick="add('${p.id}')">Add</button></div></div></article>`}
function cats(ps){let cs=['Audio','Wearables','Accessories','Cameras'];return `<section class="section page-width"><div class="section-heading"><div><p class="eyebrow">SHOP BY NEED</p><h2>Find your next essential.</h2></div><a href="/products">View all →</a></div><div class="category-grid">${cs.map((c,i)=>{let p=ps.find(x=>x.category===c)||ps[i];return `<a class="category-card" href="/products?category=${c}"><img src="${p.image}" alt=""><span><b>${c}</b><small>${['Sound that moves you','Keep your day in view','Finish your setup','Create without limits'][i]}</small></span><i>→</i></a>`}).join('')}</div></section>`}
async function shop(){let ps=await products();return page(`<section class="hero"><div class="page-width hero-inner"><div class="hero-copy"><span class="hero-kicker">NEW SEASON / 2026</span><h1>Sound, vision,<br><i>and everything</i> in between.</h1><p>Explore considered electronics that make every moment feel more connected.</p><div class="hero-cta"><button onclick="go('/products')">Shop the collection</button><button class="ghost" onclick="go('/products?category=Audio')">Explore audio</button></div><div class="hero-note"><b>01</b><span>Curated technology<br>for everyday possibility</span></div></div><div class="hero-visual"><span class="hero-orb"></span><img src="${ps[0].image}" alt="Wireless headphones"><span class="floating-label"><small>Featured now</small><b>Aurora Audio</b><em>From $110</em></span></div></div></section>${cats(ps)}<section class="section page-width"><div class="section-heading"><div><p class="eyebrow">HANDPICKED FOR YOU</p><h2>Featured technology</h2></div><a href="/products">Browse collection →</a></div><div class="product-grid">${ps.slice(0,4).map(card).join('')}</div></section><section class="promo page-width"><div><p class="eyebrow">LIMITED RELEASE</p><h2>Make your desk<br>work <i>smarter.</i></h2><p>Thoughtful gear for focus, flow and every bright idea.</p><button class="light-button" onclick="go('/products?category=Accessories')">See workspace edit</button></div><img src="${ps[2].image}" alt="Workspace"></section><section class="section page-width"><div class="section-heading"><div><p class="eyebrow">CUSTOMER FAVOURITES</p><h2>Chosen again and again</h2></div></div><div class="product-grid">${ps.slice(2).map(card).join('')}</div></section><section class="benefits"><div class="page-width benefits-grid"><div><b>↗ Free shipping</b><span>On orders over $75</span></div><div><b>▣ Protected checkout</b><span>Mock payment, safely simulated</span></div><div><b>↻ Easy returns</b><span>30-day return window</span></div><div><b>◌ Two year warranty</b><span>On selected electronics</span></div></div></section>`)}
async function listing(){let ps=await products(),z=new URLSearchParams(location.search),cat=z.get('category')||'All products',term=(z.get('q')||'').toLowerCase(),sort=z.get('sort')||'featured',items=ps.filter(p=>(cat==='All products'||p.category===cat)&&(!term||JSON.stringify(p).toLowerCase().includes(term)));if(sort==='price')items.sort((a,b)=>a.price-b.price);if(sort==='rating')items.sort((a,b)=>b.rating-a.rating);return page(`<div class="page-width listing"><div class="crumb">Home / Shop / <b>${esc(cat)}</b></div><div class="listing-head"><div><p class="eyebrow">NEXORA COLLECTION</p><h1>${esc(cat)}</h1><p>Modern essentials, selected with care. ${items.length} products available.</p></div><select id="sort-products"><option value="featured">Sort: Featured</option><option value="price" ${sort==='price'?'selected':''}>Price: low to high</option><option value="rating" ${sort==='rating'?'selected':''}>Highest rated</option></select></div><div class="catalog"><aside class="filters"><b>Filter by</b><a href="/products">All categories</a>${[...new Set(ps.map(p=>p.category))].map(c=>`<a class="${c===cat?'active':''}" href="/products?category=${c}">${c}</a>`).join('')}<hr><b>Availability</b><label><input checked type="checkbox"> In stock</label><label><input type="checkbox"> On sale</label></aside><div class="product-grid">${items.length?items.map(card).join(''):`<div class="empty-state"><h2>No products found</h2><button onclick="go('/products')">Clear filters</button></div>`}</div></div></div>`)}
async function detail(id){let p=await api('/api/products/'+id),price=p.price*(1-p.discount/100);return page(`<div class="page-width detail"><div class="crumb">Home / Products / ${p.category} / <b>${p.name}</b></div><div class="detail-grid"><section class="gallery"><div class="main-image"><span class="product-badge">${p.discount}% off</span><img src="${p.image}" alt=""></div><div class="thumbs"><button><img src="${p.image}"></button><button><img src="${p.image}"></button></div></section><section class="detail-copy"><p class="eyebrow">${p.category} / NEXORA SELECT</p><h1>${p.name}</h1><p class="detail-rating">★ ${p.rating} <u>128 verified reviews</u></p><div class="detail-price"><strong>${money(price)}</strong><del>${money(p.price)}</del><span>Save ${p.discount}%</span></div><p>${p.description}</p><p class="stock">● In stock · Ships within 1–2 business days</p><div class="detail-actions"><button onclick="add('${p.id}')">Add to cart</button><button class="ghost" onclick="go('/cart')">Buy now</button><button class="round">♡</button></div><div class="assurance"><span>▣ Secure simulated payment</span><span>↻ 30-day returns</span><span>◌ 2-year warranty</span></div></section></div><section class="detail-info"><div><b>Details</b><p>${p.description} Designed with dependable materials and intuitive everyday use in mind.</p></div><div><b>Specifications</b><p>Premium construction · Wireless connectivity · 2-year coverage</p></div><div><b>Delivery & returns</b><p>Free shipping over $75. Simple returns within 30 days.</p></div></section></div>`)}
async function cart(){let c=await api('/api/cart');return page(`<div class="page-width cart-page"><div><p class="eyebrow">YOUR BAG</p><h1>Cart <span>(${c.items.length})</span></h1>${c.items.length?`<div class="cart-list">${c.items.map(i=>`<article class="cart-item"><img src="${i.product.image}" alt=""><div><small>${i.product.category}</small><h3>${i.product.name}</h3><p>${money(i.price)}</p><button class="link-button" onclick="remove('${i.id}')">Remove</button></div><div class="qty-control"><button onclick="qty('${i.id}',${i.quantity-1})">−</button><b>${i.quantity}</b><button onclick="qty('${i.id}',${i.quantity+1})">+</button></div><b>${money(i.total)}</b></article>`).join('')}</div>`:`<div class="empty-state"><h2>Your bag is waiting.</h2><button onclick="go('/products')">Shop collection</button></div>`}</div><aside class="summary"><h2>Order summary</h2><div><span>Subtotal</span><b>${money(c.subtotal)}</b></div><div><span>Shipping</span><b>Calculated at checkout</b></div><div class="summary-total"><span>Total</span><strong>${money(c.subtotal)}</strong></div><button ${c.items.length?'':'disabled'} onclick="go('/checkout')">Secure checkout</button><p>▣ Checkout is simulated. No real payment is taken.</p></aside></div>`)}
async function checkout(){let c=await api('/api/cart');if(!c.items.length){go('/cart');return ''}return page(`<div class="page-width checkout"><section><p class="eyebrow">SECURE CHECKOUT</p><h1>Almost there.</h1><div class="checkout-steps"><b>1 Information</b><span>2 Options</span><span>3 Payment</span></div><form id="checkout-form" class="checkout-form"><h2>Delivery details</h2><label>Full name<input value="${esc(state.session?.name)}" required></label><label>Shipping address<input id="shipping-address" placeholder="Street, city, postal code" required></label><div class="network-options simulation-options" role="radiogroup" aria-label="Order options"><label><input type="radio" name="simulation" value="SAFE_NETWORK" checked><span><b>1</b></span></label><label><input type="radio" name="simulation" value="CRITICAL_NETWORK"><span><b>2</b></span></label><label><input type="radio" name="simulation" value="PAYMENT_SUCCESS"><span><b>3</b></span></label><label><input type="radio" name="simulation" value="PAYMENT_FAILURE"><span><b>4</b></span></label></div><button>Place order</button></form></section><aside class="summary"><h2>Your order</h2>${c.items.map(i=>`<div class="mini-item"><img src="${i.product.image}"><span>${i.product.name} × ${i.quantity}<b>${money(i.total)}</b></span></div>`).join('')}<div class="summary-total"><span>Total</span><strong>${money(c.subtotal)}</strong></div></aside></div>`)}
async function order(id){let o=await api('/api/orders/'+id),ok=o.status==='CONFIRMED';return page(`<div class="page-width confirmation"><div class="confirmation-icon ${ok?'okay':'not-ok'}">${ok?'✓':'!'}</div><p class="eyebrow">${ok?'ORDER COMPLETE':'ORDER NOT COMPLETED'}</p><h1>${ok?'Thank you, '+esc(o.buyerName)+'.':'Your order needs attention.'}</h1><p>${ok?'Your order is confirmed and we will keep you updated.':'The simulated payment or safety evaluation prevented this order from being confirmed.'}</p><div class="confirmation-card"><div><span>Order number</span><b>${o.id}</b></div><div><span>Payment</span><b>${o.paymentStatus}</b></div><div><span>Total</span><b>${money(o.amount)}</b></div><div><span>Placed</span><b>${new Date(o.createdAt).toLocaleDateString()}</b></div></div><button onclick="go('/orders')">View your orders</button></div>`)}
async function orders(){let os=await api('/api/orders'),items=os.length?os.map(o=>`<button class="order-row" onclick="go('/order/${o.id}')"><span><b>${o.id}</b><small>${new Date(o.createdAt).toLocaleDateString()}</small></span><span>${money(o.amount)}</span><span class="status ${o.status}">${o.status}</span>→</button>`).join(''):`<div class="empty-state"><h2>No orders yet</h2><button onclick="go('/products')">Shop now</button></div>`;return page(`<div class="page-width orders"><p class="eyebrow">YOUR ACCOUNT</p><h1>Orders</h1><div class="orders-list">${items}</div></div>`)}
function dashMenu(){return `<aside class="dash-side"><b>DASHBOARD</b><a href="/dashboard/overview">Overview</a><a href="/dashboard/logs">Logs</a><a href="/dashboard/prediction">Prediction</a></aside>`}
async function dash(section){if(!['SUPER_ADMIN','MANAGER','ANALYST'].includes(state.session?.role))return `<div class="restricted"><h1>Operations access required</h1><button onclick="go('/login')">Operations sign in</button></div>`;let d=await api('/api/dashboard/data'),body;if(section==='prediction'){body=await window.predictionContent()}else if(section==='logs'){let x=await api('/api/logs');body=`<h1>Logs</h1><div class="dash-card table-wrap"><table><thead><tr><th>Time</th><th>Product</th><th>Network metrics</th><th>Severity</th><th>AI decision</th><th>Payment</th><th>Outcome</th></tr></thead><tbody>${x.map(l=>`<tr><td>${new Date(l.timestamp).toLocaleString()}<small>${l.buyerName}</small></td><td>${l.productName||'Unknown product'}</td><td>Latency: ${l.latency}ms<small>Packet loss: ${l.packetLoss}% · Error rate: ${l.errorRate}%</small></td><td><span class="status ${l.severity}">${l.severity}</span></td><td>${l.aiDecision}<small>${l.aiReason}</small></td><td>${l.paymentStatus}</td><td>${l.finalOutcome}</td></tr>`).join('')}</tbody></table></div>`}else body=`<h1>Overview</h1><div class="metrics">${Object.entries(d.stats).map(([k,v])=>`<div class="metric"><span>${k.replace(/([A-Z])/g,' $1')}</span><b>${v}</b></div>`).join('')}</div><div class="dash-card table-wrap"><h2>Recent transactions</h2><table><thead><tr><th>Transaction</th><th>Buyer</th><th>Amount</th><th>AI</th><th>Payment</th><th>Order</th></tr></thead><tbody>${d.transactions.map(x=>`<tr><td>${x.id}</td><td>${x.buyerName}</td><td>${money(x.amount)}</td><td>${x.ai?.decision||'—'}</td><td>${x.paymentStatus||'—'}</td><td>${x.orderStatus||'—'}</td></tr>`).join('')}</tbody></table></div>`;return `<div class="dashboard-shell"><header class="dash-header"><a href="/shop">NEXORA.</a><span>Simulation dashboard</span><button onclick="go('/shop')">Open storefront</button></header><div class="dash-layout">${dashMenu()}<main class="dash-main">${body}</main></div></div>`}
window.add=async id=>{try{await api('/api/cart/items',{method:'POST',body:JSON.stringify({productId:id})});toast('Added to your bag')}catch(e){toast(e.message)}};window.qty=async(id,n)=>{if(n<1)return remove(id);await api('/api/cart/items/'+id,{method:'PATCH',body:JSON.stringify({quantity:n})});render()};window.remove=async id=>{await api('/api/cart/items/'+id,{method:'DELETE'});render()};window.saveConfig=async type=>{let x={enabled:q('#enabled').value==='true',scenario:q('#scenario').value};if(type==='network')Object.assign(x,{packetLoss:+q('#loss').value,latency:+q('#latency').value,errorRate:+q('#error-rate').value});await api('/api/config',{method:'POST',body:JSON.stringify(type==='network'?{network:x}:{payment:x})});toast('Configuration saved');render()};window.report=async()=>{let r=await fetch('/api/reports/logs/pdf',{headers:{Authorization:'Bearer '+state.session.token}}),b=await r.blob(),a=document.createElement('a');a.href=URL.createObjectURL(b);a.download='nexora-report.pdf';a.click()};window.go=go;
function bind(){let s=q('#site-search');if(s)s.onkeydown=e=>{if(e.key==='Enter')go('/products?q='+encodeURIComponent(s.value))};let w=q('#welcome-form');if(w)w.onsubmit=async e=>{e.preventDefault();try{let x=await api('/api/buyers/session',{method:'POST',body:JSON.stringify({name:q('#buyer-name').value})});state.buyerSession=x.session;state.session=x.session;localStorage.setItem('sentinelBuyerSession',JSON.stringify(x.session));localStorage.setItem('sentinelSession',JSON.stringify(x.session));go('/shop')}catch(e){toast(e.message)}};let l=q('#login-form');if(l)l.onsubmit=async e=>{e.preventDefault();try{let x=await api('/api/auth/login',{method:'POST',body:JSON.stringify({email:q('#email').value,password:q('#password').value})});state.adminSession=x.session;state.session=x.session;localStorage.setItem('sentinelAdminSession',JSON.stringify(x.session));localStorage.setItem('sentinelSession',JSON.stringify(x.session));go('/dashboard')}catch(e){toast(e.message)}};let f=q('#checkout-form');if(f)f.onsubmit=async e=>{e.preventDefault();try{let selected=q('input[name="simulation"]:checked')?.value||'SAFE_NETWORK',paymentFailure=selected==='PAYMENT_FAILURE',networkScenario=selected==='CRITICAL_NETWORK'?'CRITICAL':'SAFE';let x=await api('/api/checkout',{method:'POST',body:JSON.stringify({shipping:{address:q('#shipping-address').value},networkScenario,paymentFailure})});go('/order/'+x.order.id)}catch(e){toast(e.message)}};let sort=q('#sort-products');if(sort)sort.onchange=()=>{let z=new URLSearchParams(location.search);z.set('sort',sort.value);go('/products?'+z)}}
async function render(){let p=location.pathname,isDashboard=p==='/overview'||p==='/logs'||p.startsWith('/dashboard');if(isDashboard&&state.adminSession)state.session=state.adminSession;if(!isDashboard&&p!=='/login'&&p!=='/welcome'&&p!=='/'&&state.buyerSession)state.session=state.buyerSession;try{let h;if(p==='/'||p==='/welcome')h=`<div class="welcome"><div class="welcome-art"></div><form id="welcome-form" class="welcome-card"><a class="wordmark" href="/welcome">NEXORA<span>.</span></a><p class="eyebrow">WELCOME TO A BETTER EDIT</p><h1>Before we begin,<br>what should we call you?</h1><p>Your name personalizes your shopping session and order updates.</p><label>Your name<input id="buyer-name" minlength="2" placeholder="Enter your name" required autofocus></label><button>Continue to shop →</button><small>No account or password required.</small></form></div>`;else if(p==='/login')h=`<div class="welcome"><form id="login-form" class="welcome-card"><a class="wordmark" href="/welcome">NEXORA<span>.</span></a><p class="eyebrow">OPERATIONS ONLY</p><h1>Control centre sign in.</h1><p>Buyer shopping never requires a login.</p><label>Email<input id="email" type="email" value="admin@sentinel.test" required></label><label>Password<input id="password" type="password" value="Admin123!" required></label><button>Sign in →</button></form></div>`;else if(p==='/shop')h=await shop();else if(p==='/products')h=await listing();else if(p.startsWith('/product/'))h=await detail(p.split('/').pop());else if(p==='/cart')h=await cart();else if(p==='/checkout')h=state.buyerSession?await checkout():`<div class="restricted"><h1>Enter your name to begin checkout.</h1><button onclick="go('/welcome')">Enter buyer name</button></div>`;else if(p.startsWith('/order/'))h=await order(p.split('/').pop());else if(p==='/orders')h=await orders();else if(p==='/profile')h=page(`<div class="page-width profile"><p class="eyebrow">YOUR ACCOUNT</p><h1>${esc(state.session?.name||'Guest')}</h1><p>Buyer session active. Your cart and orders are saved for this session.</p><button onclick="signout()">End shopping session</button></div>`);else if(p==='/overview')h=await dash('overview');else if(p==='/logs')h=await dash('logs');else if(p.startsWith('/dashboard'))h=await dash(p.split('/')[2]||'overview');else h=page(`<div class="empty-state"><h1>We couldn’t find that page.</h1><button onclick="go('/shop')">Back to storefront</button></div>`);app.innerHTML=h;bind()}catch(e){app.innerHTML=`<div class="error-screen"><h1>Something needs attention.</h1><p>${esc(e.message)}</p><button onclick="go('/welcome')">Start again</button></div>`}}
window.signout=()=>{state.session=null;state.buyerSession=null;localStorage.removeItem('sentinelSession');localStorage.removeItem('sentinelBuyerSession');go('/welcome')};document.addEventListener('click',e=>{let a=e.target.closest('a[href]');if(a&&a.origin===location.origin){e.preventDefault();go(a.pathname+a.search)}});window.onpopstate=render;render();
