const crypto=require('crypto');const id=p=>p+'-'+crypto.randomUUID().slice(0,8),now=()=>new Date().toISOString();
function incidentFor(d,log){d.recoveries=d.recoveries||[];let r=d.recoveries.find(x=>x.originalOrderId===log.orderId);if(r)return r;if(!(log.moneyDebited&&log.settlementStatus==='DEBITED_FROM_CUSTOMER_NOT_RECEIVED_BY_ORGANIZATION'))return null;r={id:id('INC'),type:'PAYMENT_DEBITED_NOT_CREDITED',customerSessionId:log.sessionId,customerName:log.buyerName,originalOrderId:log.orderId,productId:log.productId,productName:log.productName,amount:(d.orders.find(x=>x.id===log.orderId)||{}).amount||0,status:'CUSTOMER_ACTION_REQUIRED',severity:'HIGH',priority:'P1',aiDecision:'CUSTOMER_RECOVERY_REQUIRED',availableActions:['REFUND','REORDER'],customerDecision:null,executionStatus:'NOT_STARTED',verificationStatus:'PENDING',createdAt:now(),updatedAt:now(),timeline:[['Payment debited; merchant not credited',now()],['AI recovery workflow initiated',now()],['Customer notified',now()]]};d.recoveries.unshift(r);return r}
module.exports={incidentFor};
